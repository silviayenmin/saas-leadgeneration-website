/**
 * Global Site & SaaS Product Configuration
 * 
 * To link your SaaS product application (e.g. login/signup or web app portal):
 * Update `saasProductUrl` to your URL (e.g. 'https://app.mapflow.ai' or '/signup').
 */
export const SITE_CONFIG = {
  // Configurable SaaS Product App URL (Internal route like '/signup' or external URL like 'https://app.mapflow.ai')
  saasProductUrl: '/signup',
  appName: 'MapFlow AI',
  contactEmail: 'madhusudhanan.yenmin@gmail.com'
};
